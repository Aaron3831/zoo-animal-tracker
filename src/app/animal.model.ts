export class Animal {
  public fed: boolean = false;
  constructor(public species: string, public name: string, public age: number, public diet: string, public location: string, public caretakeramount: number, public sex: string, public likes: string, public dislikes: string) {}
}
